# MiniPay reviews a Mini App's supply chain: it runs inside a wallet, so an
# install-time script is a path to the user's funds. Nothing in this project
# needs one.
ignore-scripts=true

# Pinned exactly in package.json as well — a caret range means the build that
# gets whitelisted is not necessarily the build that ships.
save-exact=true
